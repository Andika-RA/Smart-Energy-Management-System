apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: smartcity-energy-management-system
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      containers:
      - name: api-gateway
        image: smart-city-platform-api-gateway:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3060  # <--- PORT INTERNAL 3060
        envFrom:
        - configMapRef:
            name: smartcity-config
        - secretRef:
            name: smartcity-secrets
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  namespace: smartcity-energy-management-system
spec:
  selector:
    app: api-gateway
  ports:
    - port: 3060
      targetPort: 3060
      nodePort: 30065
  type: NodePort